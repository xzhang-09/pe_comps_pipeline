__pycache__/
*.pyc
.venv/
.DS_Store
.pytest_cache/
.env

# Phase 6 DoD: runtime artifacts (cache, checkpoints, trained models, generated
# reports, logs) are reproducible from a real pipeline run and shouldn't be
# tracked as repo state going forward.
data/cache/
data/checkpoints/
data/models/
outputs/
logs/

# Same reasoning as above — regenerated by eval/evaluator.py on every run,
# not meaningful repo state.
eval/manual_review_sample.txt

# Local Claude Code tool permissions (machine/session-specific allowlist),
# not project configuration — was accidentally committed in the first commit.
.claude/settings.local.json
instruction/
