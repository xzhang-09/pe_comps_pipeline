__pycache__/
*.pyc
.venv/
.DS_Store
.pytest_cache/
.coverage
.env

# Runtime artifacts are reproducible from a real pipeline run and should not be
# tracked as repo state.
data/cache/
data/checkpoints/
data/models/
outputs/
logs/
run_backups/

# Machine-prefilled ground-truth draft, rewritten by scripts/prefill_manual_deal.py /
# eval/ground_truth_builder.py. Only the human-audited manual_deals.json (plus the
# .example template) is repository data.
eval/ground_truth/manual_deals.review.json

# Local Claude Code configuration (machine/session-specific, not project config).
.claude/

# Build artifacts.
*.egg-info/
